<?php

declare(strict_types=1);

namespace {{ namespace }};

use Toporia\Framework\Database\Factory;
use Toporia\Framework\Database\ORM\Model;

/**
 * {{ class }}
 *
 * Factory for creating {{ modelName }} model instances.
 *
 * @template T of Model
 * @extends Factory<T>
 */
final class {{ class }} extends Factory
{
    /**
     * The name of the factory's corresponding model.
     *
     * @var class-string<T>
     */
    protected string $model = {{ model }};

    /**
     * Define the model's default attributes.
     *
     * @return array<string, mixed>
     */
    public function definition(): array
    {
        return [
            // Define your model's default attributes here
            // Example:
            // 'name' => $this->faker()->name(),
            // 'email' => $this->faker()->unique()->safeEmail(),
            // 'created_at' => now(),
            // 'updated_at' => now(),
        ];
    }

    /**
     * Indicate that the model is in a specific state.
     *
     * @return array<string, mixed>
     */
    public function state{{ stateName }}(): array
    {
        return $this->state(function (array $attributes) {
            return [
                // Define state-specific attributes
            ];
        });
    }
}
